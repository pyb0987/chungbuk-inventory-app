# 충북사무소 재고관리 프로그램 사용 안내

## 1. 프로그램 실행

폴더 안의 `ChungbukInventory.exe`를 실행합니다.

작은 실행 창이 열리고, 잠시 후 브라우저에서 재고관리 화면이 열립니다.

## 2. 중요한 주의사항

공용 데이터 폴더는 삭제하면 안 됩니다.

두 Windows 사용자 계정은 아래 폴더의 같은 데이터베이스를 사용합니다.

```text
C:\Users\Public\Documents\ChungbukInventory
```

이 폴더 안에 실제 재고 데이터, 백업, 날짜별 로그가 저장됩니다.
두 계정에서 프로그램을 동시에 실행할 수는 없습니다.

자동 업데이트가 실패하면 이전 버전을 유지하고 아래 폴더의 날짜별 업데이트 로그에
실패 원인과 복구 결과를 남깁니다.

설치 위치가 Windows에서 보호되는 폴더라면 업데이트할 때 관리자 권한 확인 창이
나타날 수 있습니다. `예`를 누르면 설치 위치를 옮기지 않고 업데이트를 계속합니다.

```text
C:\Users\Public\Documents\ChungbukInventory\logs
```

업데이트 후에는 실제 데이터가 있는 Windows 계정에서 먼저 프로그램을 실행하세요.
다른 계정에도 별도의 기존 데이터베이스가 있으면 프로그램이 자동으로 덮어쓰지 않고
충돌 안내를 표시합니다. 이 경우 두 데이터베이스를 모두 백업한 뒤 사용할 파일을
선택해야 합니다.

## 3. 처음 재고 가져오기

`가져오기` 화면으로 이동합니다.

`현재 재고 가져오기`에서 `충북사무소 재고현황 260601.xlsx` 파일을 선택하고 `가져오기`를 누릅니다.

이 파일은 현재 재고 기준값으로 반영됩니다.

## 4. 사용내역 가져오기

같은 `가져오기` 화면에서 `사용내역 가져오기`를 사용합니다.

`충북사무소 사용내역 부품신청서 2602.xlsb` 파일을 선택하고 가져올 수 있습니다.

이 사용내역은 참고 기록으로만 저장되며, 현재 재고 수량을 다시 변경하지 않습니다.

## 5. 평소 입출고 입력

`입출고` 화면에서 작업을 입력합니다.

- 시리얼 번호가 필요한 경우 `시리얼` 칸에 함께 입력합니다. 필요하지 않으면 비워 둡니다.
- `개인 출고`: 파트실 재고가 줄고 개인 보유 수량이 늘어납니다.
- `개인 반납`: 개인 보유 수량이 줄고 파트실 재고가 늘어납니다.
- `개인 설치`: 선택한 개인의 보유 수량만 줄어듭니다.
- `개인 회수`: 선택한 개인의 보유 수량만 늘어납니다.
- `서울로 반납`: 파트실 재고가 줄어듭니다.
- `서울에서 파트실로 택배`: 파트실 재고가 늘어납니다.
- `사무실 출고`: 파트실 재고가 줄고 사무실 보유 수량이 늘어납니다.
- `사무실 반출`: 사무실 보유 수량이 줄고 파트실 재고가 늘어납니다.

재고표를 개인별로 확인하거나 전달하려면 `재고표` 화면의 `엑셀로 내보내기`를
누릅니다. 품목, 파트실, 개인별 보유, 사무실, 개인/사무실 합계, 전체 합계가
포함된 `.xlsx` 파일이 저장됩니다.

## 6. 재고 직접 조정

실제 수량과 프로그램 수량이 다를 때는 `재고표` 화면의 `재고 조정`을 사용합니다.

조정 사유를 적어두면 나중에 기록에서 확인할 수 있습니다.

## 7. 입출고 시리얼 확인 및 수정

입력한 시리얼은 `입출고 내역`에서 확인하고 검색할 수 있습니다.

잘못 입력했다면 해당 입출고 행의 `수정`을 눌러 시리얼을 고칠 수 있습니다.

## 8. 백업

중요한 작업 전후에는 `백업` 화면에서 수동 백업을 만들 수 있습니다.

프로그램은 주요 변경 전에도 자동으로 백업을 생성합니다.

## 9. 종료

프로그램을 끝낼 때는 처음 실행된 작은 `충북 재고관리` 창을 닫습니다.

## 10. 문제가 생겼을 때

프로그램이 실행되지 않거나 오류가 나면 `C:\Users\Public\Documents\ChungbukInventory\logs\app.log` 파일을 확인하거나 전달해 주세요.
날짜별 로그는 같은 폴더의 `app-YYYY-MM-DD.log`에 저장되며 90일 동안 보관됩니다.

이 파일에 오류 원인이 기록됩니다.
